---
name: 'Build'

on:
  push:
    branches:
      - master
      - main
      - dev
  pull_request:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

jobs:
  build:
    name: Test build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build
        run: |
          npm install
          npm run build
      - name: HACS validation
        uses: "hacs/action@21.2.2"
        with:
          category: "plugin"
